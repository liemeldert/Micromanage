// Client-side (per-browser) UI preferences, persisted in localStorage via
// @mantine/hooks useLocalStorage. Not server state — these are cosmetic
// switches like showing the raw-YAML view.

export const SHOW_YAML_STORAGE_KEY = "mm_show_yaml";
